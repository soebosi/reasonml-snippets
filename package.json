{
  "name": "reasonml-snippets",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "test": "npm run build && jest",
    "fix": "npx refmt src/*.re __tests__/*.re --in-place"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@glennsl/bs-jest": "^0.4.2",
    "bs-platform": "^2.2.2"
  },
  "dependencies": {
    "reason-cli": "^3.1.0-linux"
  }
}
